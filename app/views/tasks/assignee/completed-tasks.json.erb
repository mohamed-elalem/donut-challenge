[
		{
			"type": "section",
			"text": {
				"type": "mrkdwn",
				"text": "You got <%= tasks.count %> completed <%= Task.model_name.human(count: tasks.count) %>"
			}
		}
    <%= render 'tasks/assignee/tasks-list', tasks: tasks %>
	]