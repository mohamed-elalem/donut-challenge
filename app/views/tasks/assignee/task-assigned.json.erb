[
	{
		"type": "context",
		"elements": [
			{
				"type": "mrkdwn",
				"text": "Task #<%= task.id%> *<%= task.content %>* has been assigned by <@<%= task.reporter.slack_id%>>"
			},
			{
				"type": "mrkdwn",
				"text": "on <%= task.created_at_formatted %>"
			}
		]
	}
]
